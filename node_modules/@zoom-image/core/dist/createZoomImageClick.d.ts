import * as _namnode_store_dist_store from '@namnode/store/dist/store';
import { ZoomedImgStatus } from './types.js';

type ZoomImageClickOptions = {
    zoomFactor?: number;
    zoomImageSource?: string;
};
type ZoomImageClickState = {
    zoomedImgStatus: ZoomedImgStatus;
};
declare function createZoomImageClick(container: HTMLElement, options?: ZoomImageClickOptions): {
    cleanup: () => void;
    subscribe: (listener: _namnode_store_dist_store.StoreListener<ZoomImageClickState>) => () => void;
    getState: () => ZoomImageClickState;
};

export { ZoomImageClickOptions, ZoomImageClickState, createZoomImageClick };
