type CropImageArg = {
    currentZoom: number;
    image: HTMLImageElement;
    positionX: number;
    positionY: number;
};
declare const cropImage: ({ image, positionX, positionY, currentZoom }: CropImageArg) => string;

export { cropImage };
