import * as _namnode_store_dist_store from '@namnode/store/dist/store';
import { ZoomedImgStatus } from './types.js';

type ZoomImageHoverOptions = {
    customZoom?: {
        width: number;
        height: number;
    };
    zoomImageSource?: string;
    zoomLensClass?: string;
    zoomTarget?: HTMLElement;
    scaleFactor?: number;
};
type ZoomImageHoverState = {
    zoomedImgStatus: ZoomedImgStatus;
    enabled: boolean;
};
type ZoomImageHoverStateUpdate = {
    enabled: boolean;
};
declare function createZoomImageHover(container: HTMLElement, options?: ZoomImageHoverOptions): {
    cleanup: () => void;
    subscribe: (listener: _namnode_store_dist_store.StoreListener<ZoomImageHoverState>) => () => void;
    getState: () => ZoomImageHoverState;
    setState: (newState: ZoomImageHoverStateUpdate) => void;
};

export { ZoomImageHoverOptions, ZoomImageHoverState, ZoomImageHoverStateUpdate, createZoomImageHover };
