import * as _namnode_store_dist_store from '@namnode/store/dist/store';
import { ZoomedImgStatus } from './types.js';

type ZoomImageMoveOptions = {
    zoomFactor?: number;
    zoomImageSource?: string;
};
type ZoomImageMoveState = {
    zoomedImgStatus: ZoomedImgStatus;
};
declare function createZoomImageMove(container: HTMLElement, options?: ZoomImageMoveOptions): {
    cleanup: () => void;
    subscribe: (listener: _namnode_store_dist_store.StoreListener<ZoomImageMoveState>) => () => void;
    getState: () => ZoomImageMoveState;
};

export { ZoomImageMoveOptions, ZoomImageMoveState, createZoomImageMove };
