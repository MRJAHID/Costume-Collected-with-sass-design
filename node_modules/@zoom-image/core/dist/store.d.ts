import { createStore } from '@namnode/store';

declare const makeImageCache: () => {
    createZoomImage: ({ src, store, img, }: {
        src: string;
        store: ReturnType<typeof createStore>;
        img: HTMLImageElement;
    }) => void;
    checkImageLoaded: (src: string) => boolean;
};
declare const imageCache: {
    createZoomImage: ({ src, store, img, }: {
        src: string;
        store: ReturnType<typeof createStore>;
        img: HTMLImageElement;
    }) => void;
    checkImageLoaded: (src: string) => boolean;
};

export { imageCache, makeImageCache };
