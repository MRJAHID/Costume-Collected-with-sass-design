import * as _namnode_store_dist_store from '@namnode/store/dist/store';

type ZoomImageWheelOptions = {
    maxZoom?: number;
    wheelZoomRatio?: number;
};
type ZoomImageWheelState = {
    currentZoom: number;
    enable: boolean;
    currentPositionX: number;
    currentPositionY: number;
};
type ZoomImageWheelStateUpdate = Partial<{
    enable: boolean;
    currentZoom: number;
}>;
declare function createZoomImageWheel(container: HTMLElement, options?: ZoomImageWheelOptions): {
    cleanup(): void;
    subscribe: (listener: _namnode_store_dist_store.StoreListener<ZoomImageWheelState>) => () => void;
    setState: (newState: ZoomImageWheelStateUpdate) => void;
    getState: () => ZoomImageWheelState;
};

export { ZoomImageWheelOptions, ZoomImageWheelState, ZoomImageWheelStateUpdate, createZoomImageWheel };
